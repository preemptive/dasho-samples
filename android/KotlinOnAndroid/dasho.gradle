dashOConfig {
    dashOHome = "${DASHO_HOME}"
    doxFilename = 'project.dox'
    includeAsInputs = ['kotlin-stdlib-jdk7.jar', 'kotlin-stdlib.jar', 'annotations.jar']
    skipManifestProcessing = true
}
android{buildTypes{debug{minifyEnabled true}}}
android{buildTypes{release{minifyEnabled true}}}
